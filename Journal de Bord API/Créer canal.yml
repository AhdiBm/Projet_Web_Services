info:
  name: Créer Canal
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/JournalAPI/api/channels
  body:
    type: json
    data: |-
      {
        "name": "Canal Jean",
        "type": "private"
      }
  auth:
    type: basic
    username: jean
    password: jean123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
