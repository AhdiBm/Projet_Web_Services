info:
  name: Se Connecter
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/JournalAPI/api/login
  body:
    type: json
    data: |-
      {
        "login": "jean",
        "pwd": "jean123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
