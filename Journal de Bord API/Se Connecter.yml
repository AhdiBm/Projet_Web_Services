info:
  name: Se Connecter
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/JournalAPI/api/login
  body:
    type: json
    data: |-
      {
        "login": "paul",
        "pwd": "paul456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
